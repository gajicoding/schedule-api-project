

## 폴더 구조

```bash
src.github.gajicoding.schedule_api_project
├── common              # 상수, 유틸
├── config              # 설정 관련 (Filter 등록 등)
├── controller          # REST API
├── data                # 요청/응답 DTO, Entity
├── exception           # 예외 클래스 및 처리
├── filter              # 인증 필터
├── repository          # DB 액세스
├── service             # 비즈니스 로직
└── ScheduleApiProjectApplication.java
```